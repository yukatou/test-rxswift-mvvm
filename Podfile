# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CryptoChecker' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CryptoChecker

  pod 'RxSwift',    '~> 4.0'
  pod 'RxCocoa',    '~> 4.0'
  pod 'Moya/RxSwift'
  pod 'Kingfisher', '~> 4.0'
  pod 'RealmSwift'

  target 'CryptoCheckerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CryptoCheckerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
